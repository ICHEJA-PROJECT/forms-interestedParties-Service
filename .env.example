# Application
HTTP_PORT=
NODE_ENV=
CORS_ORIGINS=

# Database Configuration (PostgreSQL only)
# IMPORTANTE: En producción, usa SSL/TLS para conexiones a la base de datos
DB_NAME=
DB_PORT=
DB_HOST=
DB_USERNAME=
DB_PASSWORD=

# JWT Configuration (mínimo 32 caracteres)
# IMPORTANTE: Cambia este secret en producción a un valor único y seguro
JWT_SECRET=
JWT_EXPIRES_IN=

# Rate Limiting (en milisegundos)
# Global: 50 requests por minuto
# Login: 5 intentos por 15 minutos
# POST /forms: 10 por hora
# GET endpoints: 100 por minuto
RATE_LIMIT_TTL=
RATE_LIMIT_MAX=
